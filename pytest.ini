[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .

# Output options
addopts = -v --tb=short

# Markers (for categorizing tests)
markers =
    integration: Integration tests that check real vault files
    unit: Unit tests for individual validators
